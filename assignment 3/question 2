#include <iostream>
using namespace std;

int main() {
    string s;
    cin >> s;
    int n = s.length();
    char stack[100];
    int top = -1;

    for (int i = 0; i < n; i++) stack[++top] = s[i];
    while (top != -1) cout << stack[top--];

    return 0;
}
